<?php
/**
 * ReviewController — ĐÃ BỊ XÓA LOGIC (Phase 5 Amputation)
 *
 * Route cũ: POST /review/submit  -> đã bị xóa
 * Route mới: POST /api/user/review -> api/controllers/UserReviewController.php
 *
 * Class này được giữ lại rỗng để tránh lỗi 404 nếu có link cũ tồn tại,
 * nhưng toàn bộ logic đã chuyển sang REST API.
 */
class ReviewController extends Controller {
    // Intentionally empty — See api/controllers/UserReviewController.php
}
